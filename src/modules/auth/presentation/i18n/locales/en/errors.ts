/**
 * auth.errors (en) — form-level auth error copy.
 */
export const errors = {
    generic: "Something went wrong. Please try again.",
    network: "Network error. Check your connection and try again.",
    sessionUnavailable: "We couldn't verify your session. Check your connection and try again.",
    retry: "Try again"
} as const;
