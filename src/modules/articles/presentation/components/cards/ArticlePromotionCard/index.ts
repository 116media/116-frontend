import { Hero } from "./ArticlePromotionCard.Hero";
import { Pair } from "./ArticlePromotionCard.Pair";
import { Side } from "./ArticlePromotionCard.Side";
import { Stats } from "./ArticlePromotionCard.Stats";
import { Strip } from "./ArticlePromotionCard.Strip";

/**
 * ArticlePromotionCard
 *
 * @description
 * Compound card family for the homepage promotion grid: `.Hero` (spot 1), `.Side`
 * (spot 2), `.Pair` (spot 3), and `.Strip` (gossip strip). Members are documented at
 * their own files.
 */
export const ArticlePromotionCard = { Hero, Side, Pair, Strip, Stats };
