/**
 * auth.session (en) — strings for session/expiry messaging.
 */
export const session = {
    expiredNotice: "Your session expired. Please log in again.",
    signOut: "Sign out",
    signOutAll: "Sign out everywhere"
} as const;
