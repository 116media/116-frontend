"use client";

import * as NavigationMenuPrimitive from "@radix-ui/react-navigation-menu";

/**
 * NavigationMenuItem
 *
 * @description
 * A single item within NavigationMenuList.
 * Acts as a scoping container for its trigger and content panel.
 * Rendered as a plain passthrough — no additional styling needed.
 */
export const NavigationMenuItem = NavigationMenuPrimitive.Item;
