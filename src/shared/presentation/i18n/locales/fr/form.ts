/**
 * form (fr)
 *
 * @description
 * Form field labels, placeholders and action buttons. Reserved for population as the
 * form-driven UI is built out; the namespace is established up front so the barrel is stable.
 */
export const form = {} as const;
