/**
 * actions (fr)
 *
 * @description
 * French strings for the shorts action rail, referenced as
 * `t("shorts.actions.<key>")`; `views` uses the `{{count}}` interpolation. Must
 * hold the exact same keys as the English mirror.
 */
export const actions = {
    like: "J'aime",
    bookmark: "Enregistrer",
    share: "Partager",
    watchFullVideo: "Voir la vidéo complète",
    views: "{{count}} vues"
} as const;
