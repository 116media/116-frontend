/**
 * actions (en)
 *
 * @description
 * English strings for the shorts action rail, referenced as
 * `t("shorts.actions.<key>")`; `views` uses the `{{count}}` interpolation. Must
 * hold the exact same keys as the French mirror.
 */
export const actions = {
    like: "Like",
    bookmark: "Save",
    share: "Share",
    watchFullVideo: "Watch full video",
    views: "{{count}} views"
} as const;
