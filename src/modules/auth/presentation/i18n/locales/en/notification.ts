/**
 * auth.notification (en) — toast copy for auth lifecycle events.
 */
export const notification = {
    signOutTitle: "Signed out",
    signOutDescription: "You have been signed out."
} as const;
