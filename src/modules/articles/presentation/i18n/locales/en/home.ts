/**
 * home (en)
 *
 * @description
 * English strings for the articles home/mega-menu surface. Owned by the articles
 * module presentation layer and exposed under the `home` namespace by the locale
 * barrel, so call sites reference them as `t("articles.home.<key>")`. Must hold
 * the exact same keys as the French mirror.
 */
export const home = {
    categories: "Categories"
} as const;
