/**
 * home (en)
 *
 * @description
 * English strings for the articles home/mega-menu surface, exposed under the `home`
 * namespace so call sites reference them as `t("articles.home.<key>")`. Must hold the
 * exact same keys as the French mirror.
 */
export const home = {
    categories: "Categories"
} as const;
