/**
 * home (fr)
 *
 * @description
 * French strings for the articles home/mega-menu surface, exposed under the `home`
 * namespace so call sites reference them as `t("articles.home.<key>")`. Must hold the
 * exact same keys as the English mirror.
 */
export const home = {
    categories: "Catégories"
} as const;
