/**
 * section (fr)
 *
 * @description
 * French strings for the homepage shorts section header, referenced as
 * `t("shorts.section.<key>")`. Must hold the exact same keys as the English mirror.
 */
export const section = {
    title: "Réels"
} as const;
