/**
 * exclusiveShow (fr)
 *
 * @description
 * French strings for the homepage exclusive show section, referenced as
 * `t("videos.exclusiveShow.<key>")`. Must hold the exact same keys as the
 * English mirror.
 */
export const exclusiveShow = {
    episodes: "Épisodes",
    exclusive: "Exclusif"
} as const;
