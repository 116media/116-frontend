/**
 * exclusiveShow (fr)
 *
 * @description
 * French strings for the homepage exclusive show section, mirror of the English
 * catalog. Exposed under the `exclusiveShow` namespace by the locale barrel and
 * referenced as `t("videos.exclusiveShow.<key>")`. Must stay key-aligned with
 * the English mirror.
 */
export const exclusiveShow = {
    episodes: "Épisodes",
    exclusive: "Exclusif"
} as const;
