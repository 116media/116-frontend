/**
 * section (en)
 *
 * @description
 * English strings for the homepage shorts section header, referenced as
 * `t("shorts.section.<key>")`. Must hold the exact same keys as the French mirror.
 */
export const section = {
    title: "Shorts",
    subtitle: "Quick clips to watch"
} as const;
