/**
 * validator (fr)
 *
 * @description
 * Client-side validation messages. Reserved for population as form validation is wired up;
 * the namespace is established up front so the barrel is stable.
 */
export const validator = {} as const;
